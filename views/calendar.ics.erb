BEGIN:VCALENDAR
VERSION:2.0
PRODID:-//tvcal/uniquenss//NONSGML v1.0//EN
<% @airings.each do |a| %>
BEGIN:VEVENT
DTSTAMP:<%= Time.now.ics %>
DTSTART:<%= a['AiringTime'].ics %>
DTEND:<%= (a['AiringTime'] + a['Duration'].to_i * 60).ics %>
SUMMARY:<%= a['Title'] %>
END:VEVENT
<% end %>
END:VCALENDAR